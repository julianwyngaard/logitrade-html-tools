# AGENTS.md

Instructions for AI agents working on this repository.

## Local Development Server

To test tools locally, use port 3737 (port 3000 is typically in use):

```bash
npx serve . -l 3737
```

Then open tools at `http://localhost:3737/[tool-name].html`.
